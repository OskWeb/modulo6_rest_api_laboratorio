import * as React from 'react';
import { AppLayout } from '#layouts';
import { EpisodeCollectionContainer } from '#pods/episodes';

export const EpisodeCollectionScene = () => (
  <AppLayout>
    <EpisodeCollectionContainer />
  </AppLayout>
);
